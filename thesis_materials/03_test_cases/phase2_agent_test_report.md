# 阶段二：手写 ReAct 调度引擎与垂直工具集自动化测试报告（毕业论文直接复用）

> **所属章节**：毕业论文 第六章 系统测试与验证 -> 6.2 核心调度引擎与工具集单元测试  
> **测试框架**：Python pytest 9.x + SQLite 隔离测试环境  
> **用例执行结果**：52 passed in 2.40s (通过率 100%)  
> **归档时间**：2026-09-09

---

## 一、 测试用例执行明细表

### 1. 垂直业务工具集与注册审计测试 (Tools Layer)

| 用例编号 | 测试模块 | 测试功能点 / 场景 | 输入数据 / 测试条件 | 预期输出与判定断言 | 实际结果 | 结论 |
| :---: | :--- | :--- | :--- | :--- | :---: | :---: |
| **TC-TOOL-01** | 工具中心 | 自动化元数据与 Schema 提取 | 装饰器 `@tool` 注册 8 个业务工具 | 符合 OpenAI tools payload 标准，剔除内部 db 参数 | Schema 生成规范 | 通过 |
| **TC-TOOL-02** | 工具中心 | 动态自定义工具注册 | 注册带参数与注释的自定义探针 | 准确解析类型约束与必需参数 required 列表 | 元数据完整绑定 | 通过 |
| **TC-TOOL-03** | 指标感知 | 服务器核心超温告警 (>40℃) | 查询 `DEV-SRV-201` 指标 | `temperature > 40.0`，`health_status='OVERHEAT'`，`alert=True` | 返回 43.5℃ 严重报警 | 通过 |
| **TC-TOOL-04** | 指标感知 | 服务器健康运行指标采集 | 查询 `DEV-SRV-202` 指标 | `temperature <= 40.0`，`health_status='HEALTHY'`，`alert=False` | 返回 31.5℃ 正常指标 | 通过 |
| **TC-TOOL-05** | 指标感知 | 未收录设备防御性排查 | 查询不存在编号 `DEV-NON-EXISTENT` | 返回 status='error' 及清晰提示，系统不崩溃 | 友好异常提示 | 通过 |
| **TC-TOOL-06** | 指标感知 | 离线无数据库模拟探针兜底 | `db=None` 状态下调用 `DEV-SRV-201` | 传感器指标模拟兜底，生成合法指标字典 | 模拟指标精准反馈 | 通过 |
| **TC-TOOL-07** | 工单管理 | 自动化生成工单编号与入库 | 提交包含故障现象的参数 | 自动生成 `TK-YYYYMMDD-XXXXXX` 编号，SQLite 真实入库 | 工单生成与持久化成功 | 通过 |
| **TC-TOOL-08** | 工单管理 | 多条件组装与分页检索 | 按 `status='PENDING'`, `priority='HIGH'` 查询 | 返回符合条件的工单集合，结果按时间倒序排列 | 条件过滤准确 | 通过 |
| **TC-TOOL-09** | 工单管理 | 工单状态推进与追加处置 | 推进状态至 `RESOLVED` 并追加描述 | 数据库记录成功刷新，`[追加处置]` 记录合流保存 | 状态闭环成功 | 通过 |
| **TC-TOOL-10** | 资产台账 | 多维台账模糊检索 | 关键词 `GPU` 模糊搜索 | 正确检索匹配的计算节点及其硬件规格详情 | 检索命中精确 | 通过 |
| **TC-TOOL-11** | 资产台账 | 正常资产借用与防重复借出 | 连续两次借用 `DEV-SRV-202` | 首次成功变更为 `IN_USE`，第二次被拦截返回 `conflict` | 拦截重复借用成功 | 通过 |
| **TC-TOOL-12** | 资产台账 | 资产归还入库与健康流转 | 归还 `DEV-SRV-201` 并将健康设为 `HEALTHY` | 状态恢复为 `AVAILABLE`，清空借用人，健康等级更新 | 台账流转符合预期 | 通过 |
| **TC-TOOL-13** | 统一审计 | 成功工具调用的审计留痕 | 调用 `get_server_metrics` | `audit_logs` 表自动新增记录，参数/返回值 JSON 序列化完整 | 审计持久化成功 | 通过 |
| **TC-TOOL-14** | 统一审计 | 非法参数/未知工具失败审计 | 传入非法 JSON 或未注册工具名称 | `audit_logs` 状态标记为 `FAILED`，记录错误追踪原因 | 失败行为全纪录 | 通过 |
| **TC-TOOL-15** | 边界防御 | 预警状态设备指标采集 | 查询 `DEV-UPS-201` (配电设备) | 返回 `WARNING` 状态，触发预警但非超温 | 状态映射完全精确 | 通过 |
| **TC-TOOL-16** | 边界防御 | 非法优先级容错与空工单检索 | 传入非法优先级 `SUPER_CRITICAL` | 自动降级为标准 `MEDIUM`，未命中查询返回空数组 | 容错机制健壮 | 通过 |
| **TC-TOOL-17** | 边界防御 | 空参数与不存在资产安全拦截 | 传入空白借用人或不存在编号 | 返回友好提示信息，事务安全回滚 | 边界防御健全 | 通过 |
| **TC-TOOL-18** | 边界防御 | 自动过滤模型幻觉参数 | 派发包含多余未声明参数的字典 | 自动剔除多余字段，安全执行目标函数，避免 TypeError | 参数清洗健壮 | 通过 |
| **TC-TOOL-19** | 参数别名 | 智能参数别名容错映射 | 传入 `device_id` 调用借还/工单等工具 | 自动映射为 `asset_no` / `ticket_no`，解决大模型命名漂移 | 别名自适应映射 | 通过 |
| **TC-TOOL-20** | 异常防护 | 恶劣循环引用对象审计安全 | 传入含循环引用的非标准参数结构体 | `AuditLog` 安全降级序列化入库，杜绝系统因日志持久化崩溃 | 审计鲁棒防崩溃 | 通过 |
| **TC-TOOL-21** | 高并发 | 工单号碰撞重试机制 | 模拟高频连续创建工单 | 触发防碰撞唯一性自增与 UUID 重试保障，100% 成功生成唯一编号 | 防碰撞入库成功 | 通过 |

---

### 2. 手写 ReAct 调度引擎测试 (Agent Core)

| 用例编号 | 测试模块 | 测试功能点 / 场景 | 输入数据 / 测试条件 | 预期输出与判定断言 | 实际结果 | 结论 |
| :---: | :--- | :--- | :--- | :--- | :---: | :---: |
| **TC-AGENT-01** | 对话交互 | 通用日常问候与人设指引 | 输入：“你好，你能做什么？” | 直接输出 Assistant 角色职能介绍，无多余工具调用 | 友好输出人设介绍 | 通过 |
| **TC-AGENT-02** | 会话持久 | 用户与模型会话历史双向持久化 | 对话完成后的数据库检查 | `chat_history` 表新增两条记录 (user 与 assistant) | 会话与思考链入库 | 通过 |
| **TC-AGENT-03** | 状态机 | 多步 ReAct 端到端自主闭环 | “查询 DEV-SRV-201 指标，若异常请提单” | 第1步查指标(43.5℃) -> 第2步研判超温主动提单 -> 汇总汇报 | 3步完整自主闭环 | 通过 |
| **TC-AGENT-04** | 业务联动 | 前后端与数据库真实状态突变 | 检查 ReAct 触发后的数据库 | SQLite `tickets` 表真实增加工单，`audit_logs` 记录 2 次操作 | 真实数据写入完成 | 通过 |
| **TC-AGENT-05** | 状态机 | 正常指标感知分支 | “巡检 DEV-SRV-202 运行指标” | 第1步查指标(31.5℃正常) -> 确认健康 -> 汇报正常无需提单 | 无过度提单，精准研判 | 通过 |
| **TC-AGENT-06** | 资产流转 | 驱动借用与归还登记动作 | 指令：“帮李老师借用 DEV-SRV-202” | 自动决策调用 `borrow_asset`，数据库 `borrower` 字段成功更新 | 资产调度闭环 | 通过 |
| **TC-AGENT-07** | 安全防护 | 最大推理步数截断与死锁保护 | 强制限制 `max_steps=1` 执行多步任务 | 达到步数后立即跳出 while 循环并输出安全保护提示 | 成功截断无死循环 | 通过 |
| **TC-AGENT-08** | 意图解析 | 资产台账检索意图驱动 | 指令：“查询资产台账列表” | 自动决策调用 `query_assets`，向用户汇总呈现台账设备 | 意图准确映射 | 通过 |
| **TC-AGENT-09** | 意图解析 | 工单查询意图驱动 | 指令：“查询工单列表” | 自动决策调用 `query_tickets`，向用户返回待办工单汇总 | 工具调度精确 | 通过 |
| **TC-AGENT-10** | SSE 规范 | 流式事件结构与 JSON 序列化 | 检查 `stream_run` 产出的所有事件 | 每个事件包含合法 `type` 且 100% 支持无损 JSON 序列化 | 契约标准合规 | 通过 |
| **TC-AGENT-11** | 独立运行 | 无外部 DB 注入独立执行 | `db=None` 启动 `engine.run()` | 引擎自适应运行并生成汇报，不发生空指针崩溃 | 独立运行稳定 | 通过 |
| **TC-AGENT-12** | 容灾高可用 | 商业 API 异常自动降级 Mock | 模拟 OpenAI 上游网络超时与认证失败 | 自动无感降级为确定性 Mock 大脑并完成闭环决策 | 容灾降级成功 | 通过 |
| **TC-AGENT-13** | 业务冲突 | 资产已被借出时的冲突响应 | 重复借用已被占用的设备 | 工具返回 `conflict`，引擎准确向用户反馈冲突原因 | 冲突反馈准确 | 通过 |
| **TC-AGENT-14** | 事实优先 | 不存在设备查询诚实防御 | 查询不存在的幽灵设备 `DEV-SRV-999` | 严格如实报告台账中未找到该设备，拒绝虚构健康指标 | 事实优先诚实汇报 | 通过 |
| **TC-AGENT-15** | 工单流转 | 自然语言驱动工单生命周期更新 | “帮我将工单 TK-xxx 状态更新为 RESOLVED” | 准确调度 `update_ticket_status`，数据库状态与追加处置成功刷新 | 工单闭环推进成功 | 通过 |
| **TC-AGENT-16** | 设备维保 | 自然语言驱动设备健康状态更新 | “将设备 DEV-SRV-201 健康状态更新为 HEALTHY” | 准确调度 `update_asset_health`，数据库资产健康等级成功变更为正常 | 资产状态流转成功 | 通过 |
| **TC-AGENT-17** | 手动提单 | 自然语言直接发起新建工单 | “创建工单：机房201空调突发冷媒泄漏紧急处置” | 自动识别提单意图与标题优先级，直接持久化写入工单库 | 手动提单调度闭环 | 通过 |
| **TC-AGENT-18** | 条件过滤 | 资产台账多维状态参数精准传递 | “查询当前健康状态为 OVERHEAT 的设备资产” | 自动解析抽取 `health_status='OVERHEAT'` 参数并传给工具 | 实体过滤精准 | 通过 |
| **TC-AGENT-19** | 条件过滤 | 待办工单状态参数精准传递 | “查询待办工单列表” | 自动解析抽取 `status='PENDING'` 参数并检索工单看板 | 工单状态过滤准确 | 通过 |

---

## 二、 自动化测试执行日志快照（供答辩演示直接复用）

```text
============================= test session starts =============================
platform win32 -- Python 3.14.3, pytest-9.1.1, pluggy-1.6.0
rootdir: d:\文档卷\Program language\毕设\backend
plugins: anyio-4.15.1
collected 52 items

tests/test_api.py ...............                                        [ 28%]
tests/test_react_engine.py .................                             [ 61%]
tests/test_tools.py ....................                                 [100%]

======================== 52 passed, 1 warning in 2.40s ========================
```
